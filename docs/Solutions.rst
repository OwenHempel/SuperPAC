Circuit Solutions
=================

Circuits are presented as collections of :ref:`Components` and :ref:`Nodes`. Under the hood, Circuits will iteratively replace these components in parallel and in series, until it arrives at the sum of all the components.